/*Crie uma lista de temas onde se possa adicionar um tema, 
remover um tema, atualizar um tema, reordenar a lista, 
ordenar a lista em ordem alfabética e, por fim, mostrar a lista.*/

import 'package:listas/funcoes.dart';

void main() {
  menu();
}
